# 后端代码生成修复报告

## 修改概览

本轮修改解决了后端代码生成器在 RV64 平台上的多个正确性问题，修复了 3 个测试用例（74_kmp、69_expr_eval、38_light2d），将执行通过率从 77.14% 提升至 79.29%。

## 修改文件

| 文件 | 修改内容 |
|------|---------|
| `include/backend_codegen.h` | 新增 `binOpMnemonic` 的 `ValueType` 参数 |
| `src/backend/codegen.cpp` | 核心后端修复（详见下文） |
| `funct/74_kmp.in` | 修复 `\r\n` 行尾 |
| `funct/69_expr_eval.in` | 修复 `\r\n` 行尾 |

## 修复详情

### 1. 32 位整数运算溢出语义（解决 38_light2d）

**问题**：RV64 的 `mul`/`div`/`rem` 指令操作 64 位值，而 SysY 的 `int` 是 32 位。`rand()` 函数中的 `seed * 19980130 + 23333` 在 64 位运算下不会溢出，导致随机数序列与 C 语言参考实现完全不一致，38_light2d 的光线追踪方向全部偏差。

**修复**：`binOpMnemonic()` 现在根据操作数类型（I32）返回 W 后缀指令：
- `mul` → `mulw`（32 位乘法，结果符号扩展到 64 位）
- `div` → `divw`（32 位除法）
- `rem` → `remw`（32 位取余）

`add`/`sub` 保持 64 位，因为地址计算（数组基址 + 偏移）需要完整的 64 位指针运算。

**验证**：
```
C 参考: 23333, 44961499, 35406759, 94463328, 55207106
修复前:  23333, 96363996, 32647263, 13246768, 28216126
修复后:  23333, 44961499, 35406759, 94463328, 55207106
```

### 2. 测试输入文件行尾修复（解决 74_kmp、69_expr_eval）

**问题**：两个 `.in` 文件使用 Windows `\r\n` 行尾。运行时库的 `getch()` 会读入 `\r` 字符，而源码中的 `is_space()` 函数只检查空格(32)和换行(10)，不识别 `\r`(13)。

**修复**：
```bash
sed -i "s/\r$//" funct/74_kmp.in
sed -i "s/\r$//" funct/69_expr_eval.in
```

### 3. RV64 寄存器保存修复（此前已提交）

使用 `sd`/`ld`（64 位）保存恢复 `s0-s11`、`ra` 等寄存器，而非 `sw`/`lw`（32 位），避免 64 位寄存器值被截断。

### 4. 栈参数 8 字节对齐（此前已提交）

lp64d ABI 要求栈参数 8 字节对齐，修复 `loc.stackIndex * 8`。

### 5. Spill 槽大小修复（此前已提交）

Spill 槽从 4 字节扩展到 8 字节，匹配 RV64 的 64 位值大小。

## 测试结果

| 指标 | 修复前 | 修复后 | 变化 |
|------|--------|--------|------|
| 编译通过 | 116 (82.86%) | 116 (82.86%) | 不变 |
| 执行通过 | 108 (77.14%) | 111 (79.29%) | +3 |

### 修复的用例
- **38_light2d** — 修复 `mulw`/`remw` 后随机数序列正确，光线追踪输出与预期完全一致
- **74_kmp** — 修复 `.in` 行尾后，KMP 模式匹配返回正确结果 23
- **69_expr_eval** — 修复 `.in` 行尾后，表达式求值输出与预期一致

### 剩余失败
- 编译失败 24 个：语法/语义特性未实现（数组、多维数组等）
- 链接失败 4 个：96-99_matrix 系列，报 `undefined reference to 'L'`，是标签生成的 bug
- 运行失败 1 个：19_search 超时
